[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[user]
	name = Bryant Biggs
	email = bryantbiggs@users.noreply.github.com
[core]
	editor = nano
